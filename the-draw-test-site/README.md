# The Draw — Test Funnel

Standalone sandbox for the new The Draw funnel.

## Run

```bash
npm install
npm run dev
```

This prototype is intentionally independent of Whop, auth, payments, and the production Tarot API.
